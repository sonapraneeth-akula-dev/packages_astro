{
  "name": "@sonapraneeth/notes",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "description": "Grihasetu docs/notes platform — a static MDX docs engine (Astro + MDX) split into a shared core, a feature demo and real note apps.",
  "workspaces": [
    "packages/components",
    "packages/notes-core",
    "packages/blog-core",
    "packages/plugins/*",
    "demo/*",
    "app/blog",
    "app/notes/*"
  ],
  "catalog": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/sitemap": "^3.7.3",
    "@expressive-code/plugin-line-numbers": "^0.43.1",
    "@types/node": "^26.0.1",
    "astro": "^7.0.2",
    "astro-expressive-code": "^0.43.1",
    "chokidar": "^5.0.0",
    "katex": "^0.17.0",
    "pagefind": "^1.3.0",
    "rehype-katex": "^7.0.1",
    "remark-math": "^6.0.0",
    "typescript": "^6.0.3"
  },
  "scripts": {
    "dev:demo:notes": "bun astro dev --root demo/notes",
    "dev:demo:notebooks": "bun astro dev --root demo/notebooks",
    "dev:demo:blog": "bun astro dev --root demo/blog",
    "dev:app:notes": "bun astro dev --root app/notes/go",
    "dev:app:blog": "bun astro dev --root app/blog",
    "build": "bun run build:demo:notes && bun run build:demo:notebooks && bun run build:demo:blog && bun run build:app:notes && bun run build:app:blog",
    "build:demo:notes": "bun astro build --root demo/notes && bun pagefind --site demo/notes/dist",
    "build:demo:notebooks": "bun astro build --root demo/notebooks && bun pagefind --site demo/notebooks/dist",
    "build:demo:blog": "bun astro build --root demo/blog && bun pagefind --site demo/blog/dist",
    "build:app:notes": "bun astro build --root app/notes/go && bun pagefind --site app/notes/go/dist",
    "build:app:blog": "bun astro build --root app/blog && bun pagefind --site app/blog/dist",
    "preview:demo:notes": "bun astro preview --root demo/notes",
    "preview:demo:notebooks": "bun astro preview --root demo/notebooks",
    "preview:demo:blog": "bun astro preview --root demo/blog",
    "preview:app:notes": "bun astro preview --root app/notes/go",
    "preview:app:blog": "bun astro preview --root app/blog",
    "check": "bun astro check --root demo/notes && bun astro check --root demo/notebooks && bun astro check --root demo/blog && bun astro check --root app/notes/go && bun astro check --root app/blog",
    "test": "bun test tests",
    "test:watch": "bun test --watch tests"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14"
  }
}